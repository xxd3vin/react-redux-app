import {welcomeType} from './actionType'
/*import Api from '../../common/restfulApi';
import Config from '../config'

var rootPath = Config.serverUrl;
var api = new Api(rootPath);

var url = '/getList';*/

export function getUserNumbers(callback) {
    return (dispatch, getState) => {
        /*api.getApi(url, numbers => {
            if (typeof callback === 'function') {
                callback(numbers);
            }

            dispatch(getUserNumbersSuccess(numbers));
        }, err => {
            console.log(err);
        });*/
    }
}
